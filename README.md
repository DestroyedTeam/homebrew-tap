# DestroyedTeam Homebrew Tap

Install Unified Codex Provider:

```bash
brew install DestroyedTeam/tap/ucp
ucp setup
```

For details, see the main project:
https://github.com/DestroyedTeam/unified-codex-provider
